meta {
  name: auth
  type: http
  seq: 2
}

post {
  url: {{domain}}/api/login_check
  body: json
  auth: none
}

body:json {
  {
    "email": "roy.stephanie@pasquier.net",
    "password": "roy.stephanie@pasquier.net"
  }
}

script:post-response {
  bru.setEnvVar("token", res.body.token);
}
