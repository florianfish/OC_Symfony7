{% extends 'base.html.twig' %}

{% set action = app.current_route == 'app_admin_author_new' ? 'Ajout' : 'Édition' %}
{% block title %}Administration - {{ action }} d'auteur{% endblock %}

{% block header %}{% endblock %}

{% block body %}
    <div class="bg-light">
        <div class="container">
            <div class="row justify-content-center">
                <div class="example-wrapper pt-2 text-center">
                    <a href="{{ path('app_admin_author_index') }}" class="btn btn-primary">Retour</a>
                </div>
                <div class="col-lg-8">
                    <style>
                        .example-wrapper {
                            margin: 1em auto;
                            max-width: 800px;
                            width: 95%;
                            font: 18px/1.5 sans-serif;
                        }

                        .example-wrapper code {
                            background: #F5F5F5;
                            padding: 2px 6px;
                        }
                    </style>

                    <div class="example-wrapper">
                        <h1>{{ action }} d'auteur</h1>

                        {{ form_start(form) }}
                            {{ form_widget(form) }}
                            <button type="submit" class="btn btn-primary">Envoyer</button>
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
